{
  "name": "inquirer-exit-listener",
  "version": "2.1.0",
  "description": "Adds listeners for exit events for inquirer prompts",
  "author": "Jake Johnson",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JakeJohnson05/InquirerExitListener.git"
  },
  "main": "lib/index.js",
  "files": [
    "lib/"
  ],
  "bin": {
    "show-cursor": "lib/show-cursor.js"
  },
  "scripts": {
    "start": "ts-node src/index.ts",
    "create": "npm run build && node lib/index.js",
    "prepublish": "npm run build",
    "build": "tsc",
    "pretest": "npm run build",
    "test": "xo && tsd"
  },
  "license": "ISC",
  "dependencies": {
    "cli-cursor": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "^15.12.0",
    "ts-node": "^10.0.0",
    "tsd": "^0.17.0",
    "tslib": "^2.2.0",
    "typescript": "^4.3.2",
    "xo": "^0.40.2"
  },
  "xo": {
    "fix": true
  }
}
